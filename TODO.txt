update the plugin wiki with screenshots https://wiki.jenkins-ci.org/display/JENKINS/Maven+Metadata+Plugin
add more description about each property
make it clear this uses no maven dependencies, but just the metadata published to a repository.

